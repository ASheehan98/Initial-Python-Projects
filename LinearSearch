#Search for value in array by iterating over every value

def linearsearch(arr,x):
    n = len(arr)
    for i in range(0,n):
        if arr[i] == x:
            return i
    return -1

#test
testlist = [4,2,5,7,3,10]
print(linearsearch(testlist,7))
